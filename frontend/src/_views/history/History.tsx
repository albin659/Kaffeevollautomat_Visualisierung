import React, {useState} from 'react';
import './History.css';

const History = () => {
    const [count, setCount] = useState(0)

    return (
        <div>
            
        </div>
    );
};

export default History;